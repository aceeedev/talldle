Creating Final Data
Run `notion-to-csv.ipynb`
Run `combine.ipynb` (need to change cell to use desired file names)

Probably never need to run `celeb-heights-scraper.ipynb` again.

Pushing to Firebase
Run `ordering.ipynb`
Run `upload.ipynb` (need to change cell to use desired file names)